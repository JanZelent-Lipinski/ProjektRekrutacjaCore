<Window x:Class="ProjektRekrutacjaCore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjektRekrutacjaCore.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1000">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Szukaj:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       FontWeight="Bold"/>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="25"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1"
                        Content="X"
                        Margin="2,0,0,0"
                        Command="{Binding ClearSearchCommand}"/>
            </Grid>
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  ColumnWidth="*">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Price}"/>
                <DataGridTextColumn Width="70" Header="Waluta" Binding="{Binding Currency}" />
                <DataGridTextColumn Header="Stan Magazynowy" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Jednostka" Binding="{Binding Unit}"/>
                <DataGridTextColumn Header="Kod kreskowy" Binding="{Binding Barcode}"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>